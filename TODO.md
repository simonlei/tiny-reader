# TODO

按优先级排列，想到什么记什么。「实现todo」= 每次只挑一条推进。

## 待实现

- [ ] **文章正文抓取全文**：很多源只给摘要，点开还得跳浏览器。可加一个 `fetch_full_text` 开关，服务端抓取原文页并用 readability 思路抽取正文。
- [ ] **正文 HTML 白名单清洗 + 收紧 CSP**：现在直接 `v-html` 渲染源里的 HTML，且 CSP 关掉了。需要加一层 sanitize（可用 `ammonia` crate 在服务端入库前清洗），再把 `tauri.conf.json` 的 `security.csp` 设成明确策略。
- [ ] **订阅源分组的拖拽排序 / 重命名**：目前只能在编辑对话框里改 `category` 字符串。
- [ ] **键盘导航时列表不跟随滚动的边界情况**：滚到底自动加载更多后，选中项偶尔会跳位，需要在 `move()` 里锁住滚动锚点。
- [ ] **刷新失败重试与指数退避**：现在失败只记 `last_error`，没有自动重试。
- [ ] **导出/导入时带已读状态**：OPML 只含订阅列表，已读/星标状态目前无法迁移。可加一个自定义 JSON 快照导出。
- [ ] **服务端 HTTPS / 反向代理示例**：跨公网部署时 token 是明文，需要补一份 nginx 或 Caddy 的配置示例。
- [ ] **移动端 / 窄屏布局**：三栏在窄屏下会挤，需要折叠成两栏 + 阅读页全屏。
- [ ] **搜索高亮**：目前搜索只做 SQL LIKE 过滤，命中词没有高亮。
- [ ] **按订阅源批量管理页**：源多了以后侧边栏有点长，可以做个「管理订阅源」的表格页，支持批量改分组、批量删除。

## 已实现

- [x] Tauri 2 + Vue 3 三栏布局客户端
- [x] Rust 服务端：Axum + SQLite，单 token 鉴权
- [x] 订阅源增删改查 + OPML 导入导出（支持目录分组）
- [x] 点击刷新重新拉取所有源（并发 + 进度条 + ETag/Last-Modified 条件请求）
- [x] `j` / `k` 切换上/下一篇文章（到末尾自动加载更多）
- [x] 已读 / 星标状态保存在服务端，多客户端共享
