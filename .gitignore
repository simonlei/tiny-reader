node_modules/
dist/
dist-ssr/
target/
.vite/

# tauri-build 生成的 schema，不必入库
src-tauri/gen/

# Tauri 更新器签名密钥（私钥绝不能入库，公钥可提交）
src-tauri/keys/*.key
!src-tauri/keys/*.key.pub
*.log
.DS_Store
Thumbs.db

# 服务端运行时数据（含 token，不入库）
server/config.toml
server/data/
config.toml
*.db
*.db-shm
*.db-wal

# 提交时临时使用的 commit message 文件
.commitmsg

# 助手工作区数据（会话记忆等），不入库
.workbuddy/
